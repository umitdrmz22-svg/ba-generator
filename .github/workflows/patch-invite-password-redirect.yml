name: One-time invite password redirect patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-invite-password-redirect.yml'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Patch invitation redirect
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('supabase/functions/ehs-company-admin/index.ts')
          s=p.read_text()
          old="https://umitdrmz22-svg.github.io/defidev-legal/ehs-company-admin.html"
          new="https://umitdrmz22-svg.github.io/defidev-legal/ehs-set-password.html"
          if old not in s:
              raise SystemExit('old redirect not found')
          s=s.replace(old,new,1)
          p.write_text(s)
          PY
          grep -Fq 'ehs-set-password.html' supabase/functions/ehs-company-admin/index.ts
      - name: Commit source and remove helper
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add supabase/functions/ehs-company-admin/index.ts
          git rm .github/workflows/patch-invite-password-redirect.yml
          git commit -m 'Route new EHS invites through password setup'
          git push